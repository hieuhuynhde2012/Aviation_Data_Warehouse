version: 2

sources:
  - name: raw
    database: "{{ env_var('WAREHOUSE_DB', 'aviation_dw') }}"
    schema: raw
    tables:
      - name: raw_bts_flights
      - name: raw_airports
      - name: raw_countries
      - name: raw_regions
      - name: raw_bookings
      - name: raw_payments
