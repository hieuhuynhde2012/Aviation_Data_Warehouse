name: aviation_dw
version: "1.0.0"
config-version: 2

profile: aviation_dw

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

models:
  aviation_dw:
    staging:
      +schema: staging
      +materialized: view
    intermediate:
      +schema: intermediate
      +materialized: table
    dimensions:
      +schema: mart
      +materialized: table
    facts:
      +schema: mart
      +materialized: table
    marts:
      +schema: mart
      +materialized: table
